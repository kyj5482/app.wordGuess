{
  "name": "wordguess-app",
  "version": "1.0.0",
  "private": true,
  "description": "Word Guess — iOS/Android 네이티브 앱 (Capacitor). 웹 PoC(../) 코드를 빌드로 재사용.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node tests/run-all.mjs",
    "sync": "npm run build && npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "jsdom": "^25.0.0"
  }
}
